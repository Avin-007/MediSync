
import React from 'react';
import ResponsiveRoleSelection from './home/ResponsiveRoleSelection';

// Legacy wrapper - use ResponsiveRoleSelection directly instead
const RoleSelection: React.FC = () => {
  return <ResponsiveRoleSelection />;
};

export default RoleSelection;
